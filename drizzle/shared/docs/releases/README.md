# Shared Release Notes

This folder contains one file per published shared package version.

Naming:

- `shared-vX.Y.Z.md`

Each release note should capture:

1. Summary
2. Schema changes
3. Defaults/validation changes
4. Client impact
5. Server migration action
